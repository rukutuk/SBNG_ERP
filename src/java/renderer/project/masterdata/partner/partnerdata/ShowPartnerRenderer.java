
package renderer.project.masterdata.partner.partnerdata;

import org.zkoss.zul.Listcell;
import org.zkoss.zul.Listitem;
import org.zkoss.zul.ListitemRenderer;

public class ShowPartnerRenderer implements ListitemRenderer{

	public void render(Listitem lstm, Object o) throws Exception {
		// Partner Partner = (Partner) o;

		// Listcell cell = new Listcell(Partner.getCode());
		// cell.setParent(lstm);

		// cell = new Listcell(Partner.getName());
		// cell.setParent(lstm);

		//lstm.setAttribute("data", Partner);
	}
}