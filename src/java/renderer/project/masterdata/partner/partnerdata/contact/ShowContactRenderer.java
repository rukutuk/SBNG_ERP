package renderer.project.masterdata.partner.partnerdata.contact;

import org.zkoss.zul.Listcell;
import org.zkoss.zul.Listitem;
import org.zkoss.zul.ListitemRenderer;

public class ShowContactRenderer implements ListitemRenderer{

	public void render(Listitem lstm, Object o) throws Exception {
		// Contact Contact = (Contact) o;

		// Listcell cell = new Listcell(Contact.getPersonalindex());
		// cell.setParent(lstm);

		// cell = new Listcell(Contact.getCode());
		// cell.setParent(lstm);

		// cell = new Listcell(Contact.getFirstname());
		// cell.setParent(lstm);

		// cell = new Listcell(Contact.getStreet());
		// cell.setParent(lstm);

		// cell = new Listcell(Contact.getCity());
		// cell.setParent(lstm);

		// cell = new Listcell(Contact.getPostalcode());
		// cell.setParent(lstm);

		// cell = new Listcell(Contact.getPhone1());
		// cell.setParent(lstm);

		// cell = new Listcell(Contact.getFax1());
		// cell.setParent(lstm);

		// cell = new Listcell(Contact.getEmail());
		// cell.setParent(lstm);

		// cell = new Listcell(Contact.getWebsite());
		// cell.setParent(lstm);

		//lstm.setAttribute("data", Contact);
	}
}