
package renderer.project.masterdata.partner.partnerdata.specificaddress;

import org.zkoss.zul.Listcell;
import org.zkoss.zul.Listitem;
import org.zkoss.zul.ListitemRenderer;

public class ShowSpecificAddressRenderer implements ListitemRenderer{

	public void render(Listitem lstm, Object o) throws Exception {
		// SpecificAddress specificAddress = (SpecificAddress) o;

		// Listcell cell = new Listcell(specificAddress.getAddressname());
		// cell.setParent(lstm);

		// cell = new Listcell(specificAddress.getCity());
		// cell.setParent(lstm);

		// cell = new Listcell(specificAddress.getCountry());
		// cell.setParent(lstm);

		// cell = new Listcell(specificAddress.getFax1());
		// cell.setParent(lstm);

		// cell = new Listcell(specificAddress.getPartnerindex());
		// cell.setParent(lstm);

		// cell = new Listcell(specificAddress.getPhone1());
		// cell.setParent(lstm);

		// cell = new Listcell(specificAddress.getPostalcode());
		// cell.setParent(lstm);

		// cell = new Listcell(specificAddress.getProvince());
		// cell.setParent(lstm);

		// cell = new Listcell(specificAddress.getStreet());
		// cell.setParent(lstm);

		//lstm.setAttribute("data", specificAddress);
	}
}