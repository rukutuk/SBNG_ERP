
package renderer.hrm.masterdata.payroll.fieldallowance;

import org.zkoss.zul.Listcell;
import org.zkoss.zul.Listitem;
import org.zkoss.zul.ListitemRenderer;

public class ShowFieldAllowanceMultiplierRenderer implements ListitemRenderer{

	public void render(Listitem lstm, Object o) throws Exception {
		// FieldAllowanceMultiplier fieldAllowanceMultiplier = (FieldAllowanceMultiplier) o;

		// Listcell cell = new Listcell(fieldAllowanceMultiplier.getCode());
		// cell.setParent(lstm);

		// cell = new Listcell(fieldAllowanceMultiplier.getDescription());
		// cell.setParent(lstm);

		// cell = new Listcell(fieldAllowanceMultiplier.getMultiplier());
		// cell.setParent(lstm);

		//lstm.setAttribute("data", fieldAllowanceMultiplier);
	}
}