package renderer.hrm.setting.overtimemultiplier;

import org.zkoss.zul.Listcell;
import org.zkoss.zul.Listitem;
import org.zkoss.zul.ListitemRenderer;

public class ShowOvertimeMultiplierRenderer implements ListitemRenderer{

	public void render(Listitem lstm, Object o) throws Exception {
		// OvertimeMultiplier overtimeMultiplier = (OvertimeMultiplier) o;

		// Listcell cell = new Listcell(overtimeMultiplier.getType());
		// cell.setParent(lstm);

		// cell = new Listcell(overtimeMultiplier.getHourmin());
		// cell.setParent(lstm);

		// cell = new Listcell(overtimeMultiplier.getHourmax());
		// cell.setParent(lstm);

		// cell = new Listcell(overtimeMultiplier.getMultiplier());
		// cell.setParent(lstm);

		//lstm.setAttribute("data", overtimeMultiplier);
	}
}